## 1. 题目



### 04148: 生理周期



brute force, http://cs101.openjudge.cn/practice/04148

思路：

代码：

```
id=1
while True:
    p, e, i ,d= map(int, input().split())
    if (p, e, i,d) == (-1, -1, -1,-1) :
        break
    for j in range(d+1, 212530):
        if (j - p) % 23 == 0 and (j - e) %28 == 0 and (j - i) % 33 == 0:
            print(f"Case {id}: the next triple peak occurs in {j - d} days.")
            break
    id+=1
```



![image-20241023003347742](C:\Users\wangw\AppData\Roaming\Typora\typora-user-images\image-20241023003347742.png)

### 18211: 军备竞赛



greedy, two pointers, http://cs101.openjudge.cn/practice/18211

思路：

代码：

```
p=int(input())
s=list(map(int,input().split()))
s.sort()
t=0
m=len(s)-1
if p<s[0]:
    print(0)
else:
    while t<m:
        if p-s[t]>=0:
            p -= s[t]
            t+=1
        else:
            p += s[m]
            m-=1

    if p - s[t] >= 0:
        t += 1
        p -= s[t]
    m=len(s)-1-m
    num=t-m
    print(num)
```



![image-20241022193329460](C:\Users\wangw\AppData\Roaming\Typora\typora-user-images\image-20241022193329460.png)

### 21554: 排队做实验



greedy, http://cs101.openjudge.cn/practice/21554

思路：

代码：

```
n = int(input())
a = list(map(int, input().split()))
b = list(range(1, n + 1))

# 使用 sorted 来对 (时间, 编号) 进行排序
c = sorted(zip(a, b))

s = [time for time, index in c]  # 对应的时间列表
f = [index for time, index in c]  # 对应的实验编号列表

x = 0
for i in range(len(f)):
    x += s[i] * (n-i-1)

print(*f)
print(f'{x / len(f):.2f}') 
```

![image-20241022234118022](C:\Users\wangw\AppData\Roaming\Typora\typora-user-images\image-20241022234118022.png)

### 01008: Maya Calendar



implementation, http://cs101.openjudge.cn/practice/01008/

思路：

代码：

```
n = int(input())
haab = ['pop', 'no', 'zip', 'zotz', 'tzec', 'xul', 'yoxkin', 'mol', 'chen', 'yax', 'zac', 'ceh', 'mac', 'kankin',
        'muan', 'pax', 'koyab', 'cumhu', 'uayet']
tzo = ['imix', 'ik', 'akbal', 'kan', 'chicchan', 'cimi', 'manik', 'lamat', 'muluk', 'ok', 'chuen', 'eb', 'ben', 'ix',
       'mem', 'cib', 'caban', 'eznab', 'canac', 'ahau']

results = []

for i in range(n):
    date = input().split()
    d1 = int(date[0].replace('.', '')) + 1
    m1 = haab.index(date[1]) + 1
    y1 = int(date[2])

    d2 = m1 * 20 - 20
    d3 = y1 * 365
    d = d1 + d2 + d3

    y2 = d // 260
    m2 = (d - y2 * 260) % 20
    d2_ = (d - y2 * 260) % 13
    if d2_ == 0:
        d2_ = 13
        if m2==0:
            y2 -= 1
    results.append(f"{d2_} {tzo[m2-1]} {y2}")

# 输出所有结果
print(n)
for result in results:
    print(result)

```



![image-20241023014611693](C:\Users\wangw\AppData\Roaming\Typora\typora-user-images\image-20241023014611693.png)

### 545C. Woodcutters



dp, greedy, 1500, https://codeforces.com/problemset/problem/545/C

思路：

代码：

```
import sys
 
class Node:
    def __init__(self, x, h):
        self.x = x
        self.h = h
 
def main():
    n = int(input().strip())
    
    if n == 0:
        return  # 如果 n 为 0，直接退出
    
    N = [Node(0, 0) for _ in range(n + 1)]
    
    for i in range(1, n + 1):
        x, h = map(int, input().strip().split())
        N[i] = Node(x, h)
    
    dp = [[0, 0, 0] for _ in range(n + 1)]
    
    if n >= 1:
        dp[1][1] = 1
        if n > 1 and (N[1].x + N[1].h) < N[2].x:
            dp[1][2] = 1
        else:
            dp[1][2] = 0
        dp[1][0] = 0
    
    for i in range(2, n):
        if (N[i - 1].x + N[i - 1].h) < (N[i].x - N[i].h):
            dp[i][1] = max(dp[i - 1][1], dp[i - 1][0], dp[i - 1][2]) + 1
        elif (N[i].x - N[i].h) > N[i - 1].x:
            dp[i][1] = max(dp[i - 1][1], dp[i - 1][0]) + 1
        else:
            dp[i][1] = max(dp[i - 1][1], dp[i - 1][0], dp[i - 1][2])
        
        if (N[i].x + N[i].h) < N[i + 1].x:
            dp[i][2] = max(dp[i - 1][1], dp[i - 1][0], dp[i - 1][2]) + 1
        else:
            dp[i][2] = max(dp[i - 1][1], dp[i - 1][0], dp[i - 1][2])
        
        dp[i][0] = max(dp[i - 1][1], dp[i - 1][0], dp[i - 1][2])
    
    if n == 1:
        result = 1
    else:
        result = max(dp[n - 1][1], dp[n - 1][0], dp[n - 1][2]) + 1
    
    print(result)
 
if __name__ == "__main__":
    main()
```



![image-20241027121619759](C:\Users\wangw\AppData\Roaming\Typora\typora-user-images\image-20241027121619759.png)

### 01328: Radar Installation



greedy, http://cs101.openjudge.cn/practice/01328/

思路：

代码：

```

```



代码运行截图 （至少包含有"Accepted"）

## 2. 学习总结和收获



最近的题目有思路，但总是有些边界条件考虑不全，导致WA和RTE。Oj的题也没有数据很难改错。