### sy119: 汉诺塔



recursion, https://sunnywhy.com/sfbj/4/3/119

思路：

代码：

```
def moveHanoi(n, from_rod, to_rod, mid_rod):
    if n == 0:
        return
   
    moveHanoi(n - 1, from_rod, mid_rod, to_rod)
  
    print(f"{from_rod}->{to_rod}")
    
    moveHanoi(n - 1, mid_rod, to_rod, from_rod)

n = int(input())
print(2**n - 1)
moveHanoi(n, 'A', 'C', 'B')
```



![image-20241104001625981](C:\Users\wangw\AppData\Roaming\Typora\typora-user-images\image-20241104001625981.png)

### sy132: 全排列I



recursion, https://sunnywhy.com/sfbj/4/3/132

思路：

代码：

```
def dfs(idx, n, used, temp, result):
    if idx == n + 1:  
        result.append(temp[:])  
        return
    
    for i in range(1, n + 1):
        if not used[i]:  
            temp.append(i)  
            used[i] = True  
            dfs(idx + 1, n, used, temp, result)  
            used[i] = False  
            temp.pop()  

def generate_permutations(n):
    result = []
    used = [False] * (n + 1)  
    dfs(1, n, used, [], result) 
    
    for perm in result:
        print(" ".join(map(str, perm))) 

n = int(input())
generate_permutations(n)
```



![image-20241104164649940](C:\Users\wangw\AppData\Roaming\Typora\typora-user-images\image-20241104164649940.png)

### 02945: 拦截导弹



dp, http://cs101.openjudge.cn/2024fallroutine/02945

思路：

代码：

```
def s(nums):
    if not nums:
        return 0

    dp = [1] * len(nums)
    for i in range(len(nums)):
        for j in range(i):
            if nums[i] > nums[j]:
                dp[i] = max(dp[i], dp[j] + 1)

    return max(dp)

n = int(input())
h = list(map(int, input().split()))
hl = list((i + 1, h[i]) for i in range(n))
hl.sort(key=lambda x: x[1], reverse=True)

# 提取hl中所有按hl[i][0] 从小到大的索引
indices = [item[0] for item in hl]

print(s(indices))
```



![image-20241104170949946](C:\Users\wangw\AppData\Roaming\Typora\typora-user-images\image-20241104170949946.png)

### 23421: 小偷背包



dp, http://cs101.openjudge.cn/practice/23421

思路：

代码：

```
def knapsack(N, B, values, weights):
    dp = [[0] * (B + 1) for _ in range(N + 1)]
    
    for i in range(1, N + 1):
        for w in range(1, B + 1):
            if weights[i - 1] <= w:
                dp[i][w] = max(dp[i-1][w], dp[i-1][w-weights[i-1]] + values[i-1])
            else:
                dp[i][w] = dp[i-1][w]
                
    return dp[N][B]

N, B = map(int, input().split())
values = list(map(int, input().split()))
weights = list(map(int, input().split()))

result = knapsack(N, B, values, weights)
print(result)
```



![image-20241104171637185](C:\Users\wangw\AppData\Roaming\Typora\typora-user-images\image-20241104171637185.png)

### 02754: 八皇后



dfs and similar, http://cs101.openjudge.cn/practice/02754

思路：

代码：

```

```



代码运行截图 （至少包含有"Accepted"）

### 189A. Cut Ribbon



brute force, dp 1300 https://codeforces.com/problemset/problem/189/A

思路：

代码：

```
n,a,b,c=map(int,input().split())
p=[0 for i in range(n+1)]
l=sorted([a,b,c])
for x in l:
    for i in range(x,n+1):
        if p[i-x]!=0 or i==x:
            p[i]=max(p[i],p[i-x]+1)
print(p[-1])
```



![image-20241104190944332](C:\Users\wangw\AppData\Roaming\Typora\typora-user-images\image-20241104190944332.png)

## 2. 学习总结和收获

作业较难，有的dp可以找到思路，但是很难把思路写出来，需要求助ai，有的思路也找不到。在网上找的答案都是c语言，看不明白。
